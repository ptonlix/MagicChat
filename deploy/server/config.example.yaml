server:
  addr: ":20080"

database:
  dsn: "postgres://app:app@postgres:5432/app?sslmode=disable"

admin:
  password: "change-me"

apps:
  goddess_secret: "change-me"
  goddess_websocket_url: "ws://goddess-server:20090/ws"

storage:
  provider: "s3"
  endpoint: "http://rustfs:9000"
  region: "us-east-1"
  access_key_id: ""
  secret_access_key: ""
  force_path_style: true
  buckets:
    public: "mygod-public"
    private: "mygod-private"
    temporary: "mygod-temporary"
  lifecycle:
    temporary_expire_days: 180
    abort_multipart_days: 7
