name: mygod

# Use IMAGE_REGISTRY=ghcr.1ms.run/chaitin/mygod when the GHCR mirror is needed.
services:
  postgres:
    container_name: mygod-postgres
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-app}
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-app}
    volumes:
      - ./data/postgres/data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  rustfs:
    container_name: mygod-rustfs
    image: rustfs/rustfs:latest
    restart: unless-stopped
    command: ["/data"]
    environment:
      RUSTFS_ADDRESS: ":9000"
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY:-mygod}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY:-change-me}
      RUSTFS_CONSOLE_ENABLE: "true"
    volumes:
      - ./data/rustfs/data:/data

  assistant:
    container_name: assistant
    image: ${IMAGE_REGISTRY:-ghcr.io/chaitin/mygod}/assistant:${IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      server:
        condition: service_healthy
    environment:
      MYGOD_APP_ID: 00000000-0000-0000-0000-000000000001
      MYGOD_APP_SECRET: ${MYGOD_AI_ASSISTANT_SECRET:-change-me}
      MYGOD_WS_URL: ws://server:20080/api/app/ws

  server:
    container_name: mygod-server
    image: ${IMAGE_REGISTRY:-ghcr.io/chaitin/mygod}/server:${IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      rustfs:
        condition: service_started
    environment:
      CONFIG: /app/config/config.yaml
      CLIENT_HOSTNAME: ${CLIENT_HOSTNAME:-client.localhost}
      ADMIN_HOSTNAME: ${ADMIN_HOSTNAME:-admin.localhost}
      ASSETS_HOSTNAME: ${ASSETS_HOSTNAME:-assets.localhost}
      MYGOD_AI_ASSISTANT_SECRET: ${MYGOD_AI_ASSISTANT_SECRET:-change-me}
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY:-mygod}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY:-change-me}
    volumes:
      - ./data/server/config:/app/config:ro
      - ./data/server/log:/app/log
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:20080/healthz >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  nginx:
    container_name: mygod-nginx
    image: ${IMAGE_REGISTRY:-ghcr.io/chaitin/mygod}/nginx:${IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      server:
        condition: service_healthy
      rustfs:
        condition: service_started
    environment:
      CLIENT_HOSTNAME: ${CLIENT_HOSTNAME:-client.localhost}
      ADMIN_HOSTNAME: ${ADMIN_HOSTNAME:-admin.localhost}
      ASSETS_HOSTNAME: ${ASSETS_HOSTNAME:-assets.localhost}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./data/nginx/certs:/etc/nginx/certs:ro
      - ./data/nginx/log:/var/log/nginx
    healthcheck:
      test: ["CMD-SHELL", "wget --no-check-certificate -qO- https://127.0.0.1/nginx-healthz >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
